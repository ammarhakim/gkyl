## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bldFunc = bld.shlib
    
    bldFunc(
        source = 'TenMomentImpl.cpp',
        target = 'gkeq',
        includes = '.', vnum="1.0.0"
    )    
