## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

def build(bld):
    # only build if in parallel
    bld.shlib(
        source = 'GkEulerEqn.cpp',
        target = 'gkeq', includes = '.')
