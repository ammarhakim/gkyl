## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bldFunc = bld.shlib
    
    bldFunc(source = 'test_cfuncs.cpp', includes = '.', target = 'gkunit',
            vnum="1.0")
