## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    # CUDA specific code
    if bld.env['CUTOOLS_FOUND']:
        bld.objects(source = 'test_SimpleKernel.cu', name = 'unit_cuobjs', includes='../Grid', features='c')

    bld.shlib(source = 'test_cfuncs.cpp', includes = '.',
              name = 'unit',
              vnum = '1.0',
              target = 'gkunit')
