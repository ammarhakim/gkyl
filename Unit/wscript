## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bld.shlib(source = 'test_cfuncs.cpp', includes = '.',
              name = 'unit',
              vnum = '1.0',
              target = 'gkunit')
