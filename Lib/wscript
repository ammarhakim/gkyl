## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bld.objects(source = r"""
    gkyl_ipow.cpp
    whereami.c
    lfs.c""",
    use = "LUAJIT", includes = '.', target = 'lib')
