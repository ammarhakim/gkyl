## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bld.objects(source = 'lfs.c whereami.c',
                use = "LUAJIT", includes = '.', target = 'lib')
