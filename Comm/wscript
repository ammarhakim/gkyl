## -*- python -*-
# Build code in this directory
##    _______     ___
## + 6 @ |||| # P ||| +

import os

def build(bld):
    bldFunc = bld.shlib
    
    # only build if in parallel
    if bld.env.MPI_FOUND:
        bld.objects(
            source = 'GkMpiFuncs.cpp',
            target = 'comm',
            includes = '.',
            use = 'MPI'
        )    
