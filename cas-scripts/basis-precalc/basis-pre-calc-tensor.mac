load("basis-precalc/basis-pre-calc");

for c : 1 thru 1 do (
  for v : c thru 1 do (
   fname : sconcat("~/max-out/basisTensor", c, "x", v, "v", ".lisp"),
   writeBasisToFile(c, v, 7, makeTensorBasis, fname)
  )
)$
