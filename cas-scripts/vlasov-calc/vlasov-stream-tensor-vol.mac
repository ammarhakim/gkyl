load("vlasov-calc/vlasov-stream-vol")$
load(stringproc)$

for c : 1 thru 1 do (
  for v : c thru 1 do (
   fname : sconcat("~/max-out/VlasovStreamTensor", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("VlasovVolStream", c, "x", v, "v", "Tensor"),
   calcVlasovVolUpdater(fh, funcName, c, v, "Tensor"),
   close(fh)
  )
);
