load("vlasov-calc/vlasov-em-vol")$
load(stringproc)$

for c : 1 thru 2 do (
  for v : c thru 3 do (
   fname : sconcat("~/max-out/VlasovEMMax", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("VlasovVolEM", c, "x", v, "v", "Max"),
   calcVlasovVolUpdater(fh, funcName, c, v, "Max"),
   close(fh)
  )
);

