load("vlasov-calc/vlasov-stream-surf")$
load(stringproc)$

for c : 1 thru 1 do (
  for v : c thru 1 do (
   fname : sconcat("~/max-out/VlasovSurfStreamTensor", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("VlasovSurfStream", c, "x", v, "v", "Tensor"),
   calcVlasovSurfUpdater(fh, funcName, c, v, "Tensor"),
   close(fh)
  )
);
