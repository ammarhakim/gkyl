load("vlasov-calc/single-vlasov-vol")$
load(stringproc)$

for c : 1 thru 1 do (
  for v : c thru 1 do (
   fname : sconcat("~/max-out/VlasovTensor", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("VlasovVol", c, "x", v, "v", "Tensor"),
   calcVlasovVolUpdater(fh, funcName, c, v, "Tensor"),
   close(fh)
  )
);
