load("moments-calc-c/moment-calc")$
load(stringproc)$

for c : 1 thru 2 do (
  for v : c thru 3 do (
   fname : sconcat("~/max-out/DistFuncMomentCalcSer", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("MomentCalc", c, "x", v, "v", "Ser"),
   calcMoments(fh, funcName, c, v, "Ser"),
   close(fh)
  )
);
