load("pb-calc/canonical-upwind-surf")$
load(stringproc)$

pmax : 1$
for c : 1 thru 1 do (
   v : c,
   fname : sconcat("~/max-out/CanonicalSurfSer", c, "x", v, "v", ".cpp"),
   fh : openw(fname),
   funcName : sconcat("CanonicalSurf", c, "x", v, "v", "Ser"),
   calcCanonicalPBSurfUpdater(fh, funcName, c, v, "Ser", pmax),
   close(fh)
);

